import React from "react";

const Button = (props) => {
  return (
    <>
       <button
          style={{ margin: 10 }}
          onClick={this.update}
          className="btn btn-primary"
        >
          {PaymentResponse}
        </button>
    </>
  );
};

export default Button;
